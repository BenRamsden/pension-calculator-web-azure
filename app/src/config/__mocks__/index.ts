// Mock file to avoid import.meta.env that crashes jest
// TODO= Remove need for this causes inconsitencies

export const DEFAULT_YEARLY_INTEREST_PCT = 5;
export const DEFAULT_YEARLY_PENSION_CHARGES_PCT = 0.75;
export const DEFAULT_YEARLY_INFLATION_PCT = 2.5;
export const DEFAULT_STATE_PENSION_AGE = 68;
export const DEFAULT_STATE_PENSION_WEEKLY_AMOUNT = 185.15;
export const DEFAULT_SALARY_PROJECTION_ENABLED = false;
export const DEFAULT_EARLY_DRAW_DOWN_BIAS_PCT = 0;
export const DEFAULT_LUMP_SUM_PCT = 25;
